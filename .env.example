# Leave unset to use the local sample SQLite warehouse at data/sample_warehouse.db.
# Set to point the agents at a real warehouse instead, e.g.:
#   DATABASE_URL=postgresql+psycopg2://user:password@host:5432/dbname
#   DATABASE_URL=snowflake://user:password@account/database/schema?warehouse=WH
#   DATABASE_URL=bigquery://project/dataset
DATABASE_URL=
